<%= form_for(@unidade) do |f| %>
  <% if @unidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unidade.errors.count, "error") %> prohibited this unidade from being saved:</h2>

      <ul>
      <% @unidade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :sigla %><br>
    <%= f.text_field :sigla %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
